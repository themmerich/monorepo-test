{
  "name": "domain-provision-shell",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/domain/provision/shell/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:provision", "mfe:cbs", "type:shell"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/domain/provision/shell"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
